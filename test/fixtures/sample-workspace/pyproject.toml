[build-system]
requires = ["setuptools>=67.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "sample-project"
version = "0.1.0"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
